Documentation Author: Niko Procopi 2019

This tutorial was designed for Visual Studio 2017 / 2019
If the solution does not compile, retarget the solution
to a different version of the Windows SDK. If you do not
have any version of the Windows SDK, it can be installed
from the Visual Studio Installer Tool

Welcome to the Functor Tutorial!
Prerequesites: Structures

Functors are a kind of object that overloads the () operator.
This makes it possible to use the object like a function.
For example, think about in math class how we use functions like:
f(x) = ...
This can be done with functors
